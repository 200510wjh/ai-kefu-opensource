# 2026-07-02 功能测试报告

## 测试环境

- 本地后端：`http://127.0.0.1:8000`
- 本地前端：`http://127.0.0.1:5173`
- 数据库：`data/merchant_growth.sqlite3`
- 测试账号：`ai_kefu_demo / admin123`
- 线上服务器：暂未恢复，`wjhai.cn` 本次不作为通过项

## 已通过

1. 后端健康检查
   - `GET /api/health` 返回 `ok`
   - 数据目录和 SQLite 路径正常

2. 代码与构建
   - `python -m py_compile backend/main.py backend/customer_service_saas.py` 通过
   - `npm run build` 通过
   - `npm run desktop:mac:check` 通过

3. SaaS 登录与后台接口
   - `POST /api/auth/login` 通过，能拿到 token
   - `GET /api/dashboard/overview` 通过
   - `GET /api/merchant/profile` 通过
   - `GET /api/channels` 通过
   - `GET /api/conversations` 通过

4. 网页后台 UI
   - 登录页可打开
   - 默认账号密码可登录
   - 登录后可看到总览、商品媒体工厂、会话收件箱、接入代码等入口

5. 商品媒体工厂
   - `npm run acceptance:product-media:local` 通过
   - 页面点击“生成托管方案”后，可以生成：
     - 商品主图
     - 详情图
     - 商品短视频预览
     - 上架草稿 JSON
     - 启动包/增长包/托管包报价
     - 交付步骤与合规边界
   - 本地生成文件已落到 `运营计划/今日交付包/商品媒体工厂测试`

6. 抖音小程序商品媒体逻辑
   - `npm run acceptance:miniapp-product-media` 通过
   - 主图、详情图、视频 URL 结构正常

7. 网页客服气泡
   - `GET /api/widget-test?merchant_code=WJAIKF001` 可打开测试页
   - 右下角 AI 客服按钮存在
   - 气泡可打开、可输入消息、可发送
   - 退款/价格类咨询会返回谨慎回复
   - 新会话写入数据库，刷新后可通过后台会话接口查到
   - 最新测试会话 `need_followup=true`

8. 运营交付脚本
   - `today_check` 完成，返回码 0
   - `demand_scan` 完成，返回码 0
   - `build_pack` 完成，返回码 0
   - 已生成/更新 PPT、PDF、Excel、需求雷达和客服脚本库

9. 桌面客服话术生成
   - `npm run acceptance:reply-e2e` 通过
   - 微信、企业微信、抖音私信、淘宝、拼多多、闲鱼的回复草稿逻辑均通过
   - 未出现测试禁止承诺词

## 已修复的环境问题

- `npm run desktop:diagnose` 初次失败：缺少 `win32gui`
- 已安装当前运行时依赖：
  - `pywin32==312`
  - `uiautomation==2.0.29`
  - `pytesseract==0.3.13`
- 重跑后脚本可以启动诊断，但真实窗口读取仍未通过，原因见下方

## 未通过 / 需要修复

1. 线上服务器仍不可用
   - 阿里云普通重启已执行
   - `3002/80/443` 仍是“TCP 能连上但不返回内容”
   - `22/8003` 不可用
   - `https://wjhai.cn/merchant-admin/` 仍不能作为验收通过项
   - 下一步需要进入阿里云 Workbench 或控制台终端排查服务

2. 桌面真实平台自动读取未通过
   - `npm run acceptance:real-platforms` 未通过
   - 当前可见平台窗口：
     - `微信多开`
     - `douyin-miniapp - 抖音开发者工具 [管理员]`
   - 问题：
     - 微信只读到窗口壳文字，不是真实聊天内容
     - 抖音读到的是开发者工具窗口，不是抖音私信/客服窗口
     - 企业微信、淘宝、拼多多、闲鱼窗口未打开
   - 结论：回复草稿算法可用，但“自动监听真实聊天窗口”还不能算商用通过

3. 收件箱 UI 有状态跳转问题
   - 会话接口有真实数据
   - 收件箱可显示会话和 AI 回复
   - 但页面测试中查找“标记人工接管”时，页面状态跳到了渠道配置/人工草稿区
   - 需要单独修复收件箱详情状态和按钮稳定性

4. 会话列表中文展示存在编码问题
   - API 返回的 `last_query` 中文正常
   - 页面列表里有一处显示为 `??????????????`
   - 判断是前端摘要/渲染层编码或截断逻辑问题

5. 本地脚本按钮曾出现 `runs/undefined`
   - 后端 `POST /api/local-scripts/{id}/run` 返回 `run_id` 正常
   - 日志中曾出现前端请求 `/api/local-scripts/runs/undefined`
   - 需要检查前端按钮点击后取值字段是否有兼容问题

## 假按钮 / 临时数据 / 写死内容检查

- 登录不是假登录：真实调用 `/api/auth/login`，token 可用于后续接口
- 客服气泡不是假回复：真实调用 `/api/widget/session` 和 `/api/widget/message`，并写入数据库
- 商品媒体工厂不是纯静态：点击按钮后真实请求后端，生成 SVG/MP4/HTML 产物
- 仍有演示默认数据：账号 `ai_kefu_demo`、商品 `补水保湿精华液` 是演示种子数据
- 桌面多平台自动发送未商用通过：目前只能证明草稿生成，不可宣称微信/抖音/淘宝/拼多多/闲鱼全自动读取和发送已完成

## 下一步建议

1. 先修线上服务器 SSH/Workbench，恢复 `wjhai.cn`
2. 修复收件箱按钮和中文展示
3. 修复本地脚本前端 `run_id` 读取
4. 打开真实微信聊天窗口、企业微信、千牛/淘宝、拼多多、闲鱼窗口后，再跑 `npm run acceptance:real-platforms`
5. 等真实平台读取通过后，再考虑“自动发送”；当前建议先保持人工确认发送，降低封号和误发风险
