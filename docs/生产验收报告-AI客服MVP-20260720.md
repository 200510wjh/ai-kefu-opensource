# 生产验收报告：AI 客服 MVP

日期：2026-07-20  
线上地址：https://wjhai.cn/merchant-admin/  
验收结论：**未通过完整生产端到端验收**。  

当前只证明：线上 Web 工作台、健康接口、Windows 源码/脚本压缩包下载、部分后端 API、安全修正和部署保护已完成。  
尚未证明：真实 Windows 客服窗口自动监听、AI 基于知识库生成回复并自动发送、会话落库、人工接管、重启恢复、真实 macOS 自动化。

## 1. 发布文件检查

### Windows 包

- 地址：https://wjhai.cn/merchant-admin/downloads/desktop-agent-windows.zip
- 当前 HTTP 状态：200
- 文件大小：23255 bytes
- SHA256：`A561AE4C51E2B877F5D6210FB4F10F6D63EB92EBA1C0920D8C07A6AE618286FA`
- 版本号：`package.json` 为 `0.1.0`，Agent 默认 `app_version` 为 `desktop-agent-v1`
- 构建时间：2026-07-20 21:31:26 CST，本地文件时间；ZIP 内主要文件时间为 2026-07-20 20:37-21:13
- 类型：**源码/脚本压缩包**，不是安装程序，不是 `.exe`，不是已签名桌面应用。

文件清单：

```text
desktop-agent-windows/desktop_agent/api_client.py
desktop-agent-windows/desktop_agent/cli.py
desktop-agent-windows/desktop_agent/config.py
desktop-agent-windows/desktop_agent/core.py
desktop-agent-windows/desktop_agent/local_logging.py
desktop-agent-windows/desktop_agent/normalizer.py
desktop-agent-windows/desktop_agent/safety.py
desktop-agent-windows/desktop_agent/__init__.py
desktop-agent-windows/desktop_agent/adapters/base.py
desktop-agent-windows/desktop_agent/adapters/macos.py
desktop-agent-windows/desktop_agent/adapters/mock.py
desktop-agent-windows/desktop_agent/adapters/windows.py
desktop-agent-windows/desktop_agent/adapters/__init__.py
desktop-agent-windows/scripts/desktop_agent_runner.py
desktop-agent-windows/scripts/desktop_auto_reply_listener.py
desktop-agent-windows/scripts/desktop_listener.config.example.json
desktop-agent-windows/requirements.txt
desktop-agent-windows/README.md
desktop-agent-windows/run-windows.ps1
```

证据：

```text
Windows download HTTP 200
Length 23255
SHA256 A561AE4C51E2B877F5D6210FB4F10F6D63EB92EBA1C0920D8C07A6AE618286FA
```

是否通过：**部分通过**。文件可下载且无明显密钥，但它不是可直接双击安装的客户端。

### macOS 包

- 当前状态：**已下线**
- 当前地址：https://wjhai.cn/merchant-admin/downloads/desktop-agent-macos.zip
- 当前 HTTP 状态：404
- 页面状态：不再包含 `desktop-agent-macos.zip` 下载链接，只显示 `macOS Pending`
- 类型：无当前可下载包。此前的 macOS ZIP 是源码/脚本包，不是 App，不是 DMG，未签名，未做真实 macOS 端到端验收。

证据：

```text
macOS URL HTTP 404
Frontend bundle HasMacZip=False
Frontend bundle HasMacPending=True
```

是否通过：**未通过**。macOS 暂未完成，下载入口已移除，未上传占位 ZIP 冒充可用客户端。

## 2. Windows 实机验收

要求环境：全新 Windows 10/11、真实企业账号、真实测试客服窗口、真实消息、真实自动发送。  
实际结果：**未完成**。

| 项目 | 实际结果 | 证据 | 是否通过 | 未通过原因 | 修复内容 |
| --- | --- | --- | --- | --- | --- |
| 下载并解压 | 线上 ZIP 可下载 | HTTP 200，SHA256 见上 | 部分通过 | 未在全新 Windows 10/11 机器完成 | 待真实 Windows 测试机 |
| 启动客户端 | 未完成真实启动验收 | 无真实 Windows 客户端日志 | 未通过 | 当前包是脚本包，需要 Python 环境 | 需制作安装器或准备客户测试机 |
| 企业账号登录 | 未完成客户端登录验收 | 默认密码已失效 | 未通过 | 新强密码已生成，但未在客户端实机登录 | 用服务器 root-only 凭据文件取新测试账号 |
| 连接线上服务器 | 未完成客户端实机连接 | 后端健康接口 200 | 未通过 | 不能用接口健康代替客户端连接 | 待实机客户端日志 |
| 上传知识库 | 未完成真实上传 | 无客户知识库上传日志 | 未通过 | 未用真实企业账号上传知识库 | 待客户测试资料 |
| 监听真实客服窗口 | 未完成 | 无窗口监听日志 | 未通过 | 无真实客服平台窗口和测试账号 | 待测试客服窗口 |
| 接收真实测试消息 | 未完成 | 无真实消息证据 | 未通过 | 无真实客户消息来源 | 待测试消息 |
| AI 基于知识库生成回复 | 未完成真实闭环 | 仅有离线验收，不作为生产通过证据 | 未通过 | 未用真实知识库和窗口消息验证 | 待实机闭环 |
| 自动输入并发送 | 未完成 | 无发送截图或日志 | 未通过 | 未在真实窗口自动发送 | 待实机验证 |
| 会话/回复/日志落库 | 未完成真实闭环 | 后端表/API 存在，但无真实业务日志 | 未通过 | 不能用表存在代替业务落库 | 待实机消息 |
| 暂停/人工接管 | 未完成实机验证 | 后端 pause API 存在 | 未通过 | 未在真实客户端验证 | 待实机验证 |
| 重启后恢复 | 未完成 | 无重启日志 | 未通过 | 未做实机重启测试 | 待实机验证 |
| 同消息不重复发送 | 未完成真实窗口验证 | 后端幂等逻辑存在 | 未通过 | 未做真实重复消息发送测试 | 待实机验证 |

结论：Windows 只能算“发布包准备完成”，不能算“客户端和业务闭环可用”。

## 3. macOS 实机验收

实际结果：**macOS 暂未完成**。

| 检查项 | 实际结果 | 证据 | 是否通过 | 未通过原因 | 修复内容 |
| --- | --- | --- | --- | --- | --- |
| 辅助功能权限 | 未验收 | 无 macOS 实机 | 未通过 | 没有真实 macOS 环境 | 下载已下线 |
| 屏幕录制权限 | 未验收 | 无 macOS 实机 | 未通过 | 没有真实 macOS 环境 | 下载已下线 |
| OCR/窗口识别 | 未验收 | 无 macOS 实机 | 未通过 | 没有真实 macOS 环境 | 下载已下线 |
| 自动输入和发送 | 未验收 | 无 macOS 实机 | 未通过 | 没有真实 macOS 环境 | 下载已下线 |
| 应用签名/系统提示 | 未验收 | 无 App/DMG | 未通过 | 当前没有签名 App | 下载已下线 |

结论：macOS 不允许对外声明可用。

## 4. 业务闭环验收

目标链路：

```text
客户发送消息
-> Desktop Agent 识别
-> 请求线上 AI
-> 检索知识库
-> 生成回复
-> 风险判断
-> 自动发送
-> 会话落库
-> 工作台可查看
-> 人工可接管
```

实际结果：**未完成真实黑盒闭环**。

证据：

```text
/api/v1/health ok
Windows package HTTP 200
Desktop Agent backend API 曾通过离线 acceptance
```

是否通过：**未通过**。  
未通过原因：没有真实客服窗口、真实客户消息、真实自动发送截图/日志、真实落库记录。  
修复内容：暂停新增功能，下一步只做真实 Windows 企业测试机验收。

## 5. 安全检查

| 项目 | 实际结果 | 证据 | 是否通过 | 未通过原因 | 修复内容 |
| --- | --- | --- | --- | --- | --- |
| `admin/admin123` | 已失效 | 登录返回 401 | 通过 | - | 已更换强密码 |
| `ai_kefu_demo/admin123` | 已失效 | 登录返回 401 | 通过 | - | 已更换强密码 |
| 新凭据保存 | 保存在服务器 root-only 文件 | `/root/merchant-ai-kefu-mvp-credentials-20260720.txt`，权限 600 | 通过 | - | 不写入仓库 |
| 客户端包密钥扫描 | 未发现明显密钥 | `SECRET_HITS 0` | 通过 | - | - |
| 企业数据隔离 | 代码按 merchant_id 查询 | 未做跨企业黑盒攻击测试 | 部分通过 | 缺少真实多企业测试 | 待补充 |
| 客户端令牌过期 | 当前 token 未强制过期 | `parse_token` 未检查过期时间 | 未通过 | token 缺少过期检查 | 待修复 |
| 客户端令牌可撤销 | 未发现撤销机制 | 无 token revoke 表/API | 未通过 | token 不可撤销 | 待修复 |
| 紧急暂停 | 后端 pause API 和前端按钮存在 | `/desktop-agent/pause`，页面有 Pause | 部分通过 | 未做实机客户端验证 | 待实机验证 |
| 发送频率限制 | 本地和后端有部分限制 | `min_send_gap_seconds`、幂等 hash | 部分通过 | 未做真实重复发送测试 | 待实机验证 |

## 6. 部署保护

### 数据库备份

- 路径：`/opt/backups/ai_saas-20260720-2132-ai-kefu-mvp.sql.gz`
- 大小：83K
- 结果：通过

### 代码备份

- 路径：`/opt/backups/merchant-growth-canvas-pre-20260720-2132-ai-kefu-mvp.tar.gz`
- 大小：53M
- 结果：通过

### 回滚命令

```bash
systemctl stop merchant-growth-canvas.service
cd /opt/merchant-growth-canvas
find . -mindepth 1 -maxdepth 1 ! -name data ! -name venv ! -name .env ! -name logs -exec rm -rf {} +
tar -xzf /opt/backups/merchant-growth-canvas-pre-20260720-2132-ai-kefu-mvp.tar.gz -C /opt/merchant-growth-canvas
gunzip -c /opt/backups/ai_saas-20260720-2132-ai-kefu-mvp.sql.gz | mysql --socket=/var/run/mysqld/mysqld.sock -u root ai_saas
systemctl start merchant-growth-canvas.service
```

### 版本提交/标签

- Commit：已创建，最终以版本标签 `ai-kefu-mvp-prod-20260720` 指向的提交为准。
- Tag：`ai-kefu-mvp-prod-20260720`
- 状态：已创建。

## 7. 最终结论

本次生产验收结论：**不通过完整真实企业试用门禁**。

已完成：

- 线上 Web 和健康接口可访问。
- Windows 源码/脚本包可下载，且未发现明显密钥。
- macOS 下载入口已下线。
- 默认弱密码已更换。
- 数据库和代码备份已完成。

未完成：

- Windows 全新实机端到端自动发送。
- macOS 实机自动化。
- 真实知识库业务闭环。
- token 过期和撤销。
- 多企业隔离黑盒测试。

在完成真实端到端自动发送测试前，**不得声明“可以让真实企业客户开始测试”**。
